package ThrowawayCode;

import java.util.Random;

public class BalancedBeeFactory {
    /*private int beeTypeIndex;

    enum BeeTypes{
        Worker,Queen,Spelling,Quilting
    }

    public Bee createBee(){
        Bee bee = null;
        if(beeTypeIndex >= BeeTypes.values().length)
            beeTypeIndex = 0;
        String beeString = BeeTypes.values()[beeTypeIndex++].toString();
        trybee = (Bee)Class.forName(beeString).newInstance();
        catch
    }

    public BalancedBeeFactory(){
        this.beeTypeIndex = 0;
    }*/
}
